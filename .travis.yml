language: node_js

node_js:
  - 8
  - 6

jobs:
  include:
    - stage: test
      env:
        - NODE_ENV=development
      node_js: lts/*
      script:
        - npm t
    - stage: release
      env:
        - NODE_ENV=production
      node_js: lts/*
      script:
        - npm semantic-release
